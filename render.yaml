services:
  - type: web
    name: sertraline-predictor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run project.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

